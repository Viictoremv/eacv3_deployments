apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: parent-dev
  namespace: argocd
spec:
  project: dev
  source:
    repoURL: git@github.com:Early-Access-Care-LLC/easv3_deployments.git
    targetRevision: main
    path: apps/parent/helm-chart
    helm:
      valueFiles:
        - values.dev.yaml   # <-- this file must live in the chart root
  destination:
    server: https://eaceas-d9ansove.privatelink.eastus.azmk8s.io:443
    namespace: parent-dev
  syncPolicy:
    # automated: {}           # leave empty for manual sync; remove if you want auto
    syncOptions:
      - CreateNamespace=true
      - RespectIgnoreDifferences=true
  ignoreDifferences:
    - group: ""
      kind: Secret
      jsonPointers:
        - /data/tls.crt
        - /data/tls.key
      # limit by label so we only ignore cert-manager TLS secrets
      managedFieldsManagers:
        - cert-manager