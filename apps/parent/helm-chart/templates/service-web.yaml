apiVersion: v1
kind: Service
metadata:
  name: {{ include "eas-app-v3-parent.fullname" . }}-web
  labels:
    {{- include "eas-app-v3-parent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.web.type }}
  selector:
    app: {{ include "eas-app-v3-parent.name" . }}-web
  ports:
    - protocol: TCP
      port: {{ .Values.service.web.port }}
      targetPort: 80
