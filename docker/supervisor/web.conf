[supervisord]
nodaemon=true

[program:rsync_watch]
command=bash /rsync-watch.sh
autorestart=true
stdout_logfile=/dev/stdout
stderr_logfile=/dev/stderr
priority=10

[program:httpd]
command=/usr/sbin/httpd -D FOREGROUND
priority=20
